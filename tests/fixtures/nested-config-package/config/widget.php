<?php

declare(strict_types=1);

// Flat config file (the package short name). Mounts the legacy way via
// hasConfigFile(), proving backward compatibility alongside nested configs.
return [
    'enabled' => true,
];
